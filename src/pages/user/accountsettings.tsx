import styles from './user.module.css';

const AccountSettings = () => {
  return (
    <div>
      <h1 className={styles.home}>AccountSettings</h1>
    </div>
  );
};

export default AccountSettings;
